# PickleballSeeding

In order to make sure the app works, you do have to connect your account to Google's OAUTH2 and download the json that comes with linking your account. Drag it into the same directory as the script while also renaming it to "client_file.json". Once done, input the sheet's name, and voila, you now have a seeded pickleball tournament.
